// importing and exporting schemaTypes and resolvers for use elsewhere
const schemaTypes = require("./schemaTypes");
const resolvers = require("./resolvers");

module.exports = { schemaTypes, resolvers };
